{
  "name": "@vincemakes/boss-call",
  "version": "0.2.0",
  "description": "One Boss, several agent sessions, one line each — a mailbox for agents that cannot type into each other's terminals. A skill plus a CLI: works in Claude Code, Codex, pi, opencode and kiso through the repo's AGENTS.md / CLAUDE.md.",
  "license": "MIT",
  "type": "module",
  "bin": { "boss-call": "bin/boss-call.mjs" },
  "files": ["bin", "src", "SKILL.md", "README.md"],
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test"
  },
  "repository": { "type": "git", "url": "https://github.com/vincemakes/model-boss", "directory": "boss-call" },
  "keywords": ["agents", "kiso", "claude-code", "codex", "mailbox", "orchestration"]
}
