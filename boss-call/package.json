{
  "name": "@vincemakes/boss-call",
  "version": "0.2.0",
  "description": "One Boss, several agent sessions, one line each — a mailbox for agents that cannot type into each other's terminals. Works as a kiso extension, a Claude Code skill and a Codex skill.",
  "license": "MIT",
  "type": "module",
  "bin": { "boss-call": "bin/boss-call.mjs" },
  "files": ["bin", "src", "kiso-extension.mjs", "SKILL.md", "README.md"],
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test"
  },
  "repository": { "type": "git", "url": "https://github.com/vincemakes/model-boss", "directory": "boss-call" },
  "keywords": ["agents", "kiso", "claude-code", "codex", "mailbox", "orchestration"]
}
