name = "token_saver_scout"
description = "Read-only reconnaissance agent for the Token Saver default OpenAI profile."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = """
The host main loop remains inherited; this file configures only a spawned scout. Work
read-only and never write, format, install, or change repository state. Return concise
conclusions, file references with line numbers, current interfaces, reusable helpers,
and unresolved facts. Do not paste file bodies or the full conversation.
"""
